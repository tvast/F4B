{
  "name": "F4B_health_check",
  "private": true,
  "workspaces": [
    "front",
    "back"
  ],
  "version": "1.0.0",
  "description": "a microservice to handle health check of the conversion funnel",
  "main": "index.js",
  "author": "TOLSBLK",
  "license": "MIT",
  "scripts": {
    "start": "pm2-runtime start ecosystem.config.js --env production",
    "dev": "concurrently \"yarn workspace back run start:dev\" \"yarn workspace front dev\"",
    "heroku-postbuild": "yarn install && yarn workspace front build && yarn workspace back build && mkdir -p back/public && cp -r front/dist/* back/public/"
  },
  "devDependencies": {
    "concurrently": "^7.0.0"
  },
  "dependencies": {
    "pm2": "^5.4.3"
  },
  "packageManager": "yarn@4.7.0"
}
